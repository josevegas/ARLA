#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../semver/bin/semver.js" "$@"
else
  exec node  "$basedir/../../../semver/bin/semver.js" "$@"
fi
# cmd-shim-target=C:/Users/WINDOWS/Documents/ARLA/backend/node_modules/.pnpm/ts-jest@29.4.9_@babel+core@_b3599116768fb464a9ae11e62f1c8918/node_modules/semver/bin/semver.js
